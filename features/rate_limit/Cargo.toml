[package]
name = "rate_limit"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
libc = "0.2"
once_cell = "1.19"
parking_lot = "0.12"

[features]
default = ["plugin"]
plugin = []